"use strict";

let a = 2;
/*
    1. Операция в скобках выполняется первой и даёт в результате значение 4 (a = a * 2)
    2. Следующая операция суммирование 4 + 1 = 5
    3. Присваивание результата 5 переменной x
*/
let x = 1 + (a *= 2);